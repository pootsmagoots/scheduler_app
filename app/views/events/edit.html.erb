<h2>Edit Event</h2>

<%= form_for @event do |f| %>
 <%= f.label :name %>
 <%= f.text_field :name %>


 <%= f.submit %>
 <%= link_to 'Delete!', month_event_path(@month, @event), method: :delete, class: 'delete' %>

<%= render 'form' %>
 <% end %>
